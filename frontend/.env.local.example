# frontend/.env.local.example
# Template LOCAL untuk Nuxt (boleh berisi nilai yang tidak ingin dishare).
# Jangan commit frontend/.env.local jika berisi data sensitif.

# Internal API base url untuk server-side/proxy Nuxt (Docker)
NUXT_INTERNAL_API_BASE_URL=http://backend:5010/api

# Override API base url browser jika perlu (opsional)
NUXT_PUBLIC_API_BASE_URL=/api

# Base URL local/dev
NUXT_PUBLIC_APP_BASE_URL=http://localhost:3010
NUXT_PUBLIC_EXTERNAL_BASE_URL=http://localhost:3010

# Midtrans (untuk dev/local, client key tetap public tapi bisa beda)
NUXT_PUBLIC_MIDTRANS_CLIENT_KEY=
NUXT_PUBLIC_MIDTRANS_ENV=sandbox

# Dev bypass token (hanya untuk dev)
NUXT_PUBLIC_DEV_BYPASS_TOKEN=
